A simple HTTP proxy server in c/c++

the idea was to have a simple proxy server so anyone can
modify the code and hack around with websites.

e.g. the source sent to your browser before it is displayed,
especially useful when you want to do some javascript hacks =)
